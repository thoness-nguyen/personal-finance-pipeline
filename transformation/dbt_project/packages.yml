# packages.yml
# =============================================================================
# File: transformation/dbt_project/packages.yml
# Purpose: Declares dbt package dependencies for the finance_pipeline project.
#          Run `dbt deps` to install packages listed here into dbt_packages/.
# =============================================================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
